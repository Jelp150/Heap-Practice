#include <iostream>
#include<vector>

using namespace std;

int main()
{
	vector <int> arr;
	int num;
	int operation; 
	int q;
	cin >> q;
	
	for (int i = 0; i < q; i++)
	{
		cin >> operation;
		if (num == 1)
		{
		  cin >> num;
          arr.push_back(num); 
			
		}
		else if (num == 2)
		{
			cin >> num;
			
		}
		else if (num == 3)
		{

		}
	}

}

